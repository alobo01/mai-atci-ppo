torch>=1.10.0       # Or specify your required version
gymnasium[accept-rom-license]>=0.28.0 # Includes Atari-py for CarRacing ROMs (accept license during install or manually)
gymnasium[mujoco]>=0.28.0 # Uncomment if running MuJoCo envs
numpy>=1.20.0
matplotlib>=3.5.0
imageio>=2.15.0
imageio-ffmpeg      # Backend for imageio video writing
opencv-python>=4.5.0 # For cv2 text overlay